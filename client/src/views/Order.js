import React, { useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import axios from 'axios';

function Order({id}) {
  return (
    <div>
      This is where order details will be shown! Make sure to add id-specific calls or import the data!
    </div>
  );
}

export default Order;
